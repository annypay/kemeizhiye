# 江西柯美纸业有限公司 · 总经办文档库

> **项目全称**：莲花柯美纸业项目（5600 纸机新建、4600/3960 纸机搬迁技改、电站、水处理、制浆等配套工程）  
> **文档归属**：总经办 · 漆江峘（总经理助理）｜**总经理**：贾少军  
> **维护方式**：Git 版本管理｜远程仓库：[github.com/annypay/kemeizhiye](https://github.com/annypay/kemeizhiye)  
> **顶层目录**：`总经办文档/`｜**全文索引**：[INDEX.md](INDEX.md)（脚本自动生成，勿手改）

---

## 一、目录分类导航

全部文档按职能域分为 10 个数字前缀目录 + 1 个归档区，固定排序：

| 编号 | 目录 | 内容 | 关键文件/说明 |
| --- | --- | --- | --- |
| **00** | [`00-临时存放/`](00-临时存放/README.md) | 拟稿区：未定稿、待确认、待归档的文件 | 使用规则见目录内 README |
| **01** | [`01-进度计划/`](01-进度计划/) | 各专业月度计划表 + 模板 | `_模板-月度工作计划表_模板v2.xlsx` 为权威模板 |
| **02** | [`02-合同/`](02-合同/) | 全部合同 PDF/Word，按四个维度分箱 | 设备与技术服务 / 土建钢结构-柯美直签 / 土建分包-铭和 / 施工与装修合同 |
| **03** | [`03-例会汇报/`](03-例会汇报/董事长例会/) | 董事长周例会：PPT、各专业汇报、会议录音 | 新周资料按 `YYYYMMDD-YYYYMMDD/` 归档；历史目录保留 |
| **04** | [`04-进度督察/`](04-进度督察/) | 重大节点追踪：工作事项台账、节点汇总、通报与处罚存档 | [工作台账](04-进度督察/工作台账/README.md) 是执行状态来源；核心流程见 `通报与处罚存档/重大节点进度督察工作流程.html` |
| **05** | [`05-政府沟通/`](05-政府沟通/) | 政府侧协调事项与发函 | |
| **06** | [`06-人事行政/`](06-人事行政/) | 调查报告、通报批评、重要工作推进单 | |
| **07** | [`07-采购跟踪/`](07-采购跟踪/) | 各专业采购进度跟踪 + 汇总模板 | |
| **08** | [`08-安保与应急/`](08-安保与应急/) | 保安管理（规范+日报）+ 应急预案 | 三格式分工：md 源头 / docx 打印版 / xlsx 填报版 |
| **09** | [`09-外部调研/`](09-外部调研/单位情况/) | 外部企业信息调研 | `公司信息汇总v3.xlsx` 为权威汇总 |
| **参考资料** | [`参考资料/`](参考资料/README.md) | 静态参考资料区（组织架构、岗位说明书等） | `岗位说明书5.10/` 原始资料及 [组织架构总览](参考资料/岗位说明书5.10/20260827/组织架构总览.html) |
| — | [`_会话记录/`](_会话记录/README.md) | Copilot 会话历史与记忆快照 | 由 `scripts/sync_chat.js` 同步，不手改、不入全文索引 |
| — | [`_archive/`](_archive/) | 历史归档：旧版本、过月文件、副本存档 | 不参与日常检索，保留 Git 历史 |

**根目录只允许存在**：`README.md`、`INDEX.md`、`AGENTS.md`、`.gitignore`、`.gitattributes`、`.markdownlint.json`、`.github/`（Agent 定制）和上述目录。任何新文件先进 `00-临时存放/`，定稿后入库。

---

## 二、文档生命周期工作流

```text
起草（00-临时存放） → 你确认（【待归档】） → Agent 入库 → 提交推送
        │                    │                      │
   状态标记：              改口说"入库"        1. 按内容归入 01~09
   【待编辑】【待确认】                         2. 正式命名（见第五节）
   【待归档】                                   3. 你手改了 Office 版 → 以终稿为准回写 md
                                               4. 运行 gen_index.js 更新 INDEX.md
                                               5. git commit + push
```

**三条铁律**：① 临时区文件不自动提交 git；② 归档以你的终稿为准（Office 改了回写 md，没改用 md 生成 Office 版）；③ 超过 30 天未归档的文件由 Agent 提醒处置。

**事项闭环**：可执行决策、计划节点、推进反馈和督察通报统一关联 [工作事项总台账](04-进度督察/工作台账/工作事项总台账.md)。只有复查日期和可定位证据/归档路径齐全后，事项才可标为“已关闭”。

---

## 三、人员与专业映射

| 负责人 | 专业/领域 | 角色 |
| --- | --- | --- |
| **贾少军** | 总经理 | 全面管理 |
| **漆江峘** | 总经理助理 / 文档库归属者 | 总经办日常运转、文档治理 |
| **王隆春** | 4600 纸机 | 造纸车间主管 |
| **严金理** | 5600 机修安装 | 机修负责人 |
| **魏维久** | 5600 造纸安装 | 造纸安装负责人 |
| **施建林** | 制浆 | 制浆负责人 |
| **邬汉平** | 污水处理 | 污水站负责人 |
| **王景** | 电站 / 电站采购 | 电站负责人 |
| **刘义田** | 工艺技术 / 图纸 | 工艺技术 |
| **段伟** | 综合计划 / 电器电仪采购 / 电仪周例会汇报（20260819 起） | 综合计划编制 |
| **赖永江** | 电站电气采购 | 采购跟踪 |
| **陶总 / 张总 / 邹总 / 罗总** | 项目部/生产部 | 管理层审批 |
| **陈珂**（文件中也见「陈可」） | 采购 | 设备调运、采购部周例会汇报 |
| **陈立莎 / 刘萍萍** | 行政 | 行政周例会汇报 |
| **罗潜** | 项目部 | 周例会汇报（8.18 起） |
| **陶祥存 / 邹金艳** | （待确认） | 周例会汇报（8.24 起） |
| **（待确认）** | 机械 / 生产 / 财务部 | 周例会汇报人 |

---

## 四、工程组件索引

| 工程组件 | 关联位置 |
| --- | --- |
| **5600 纸机车间**（新建） | `01-进度计划/*5600*`、`02-合同/`（百世德钢结构）、`07-采购跟踪/*5600*` |
| **4600 纸机**（搬迁技改） | `01-进度计划/*4600*`、`02-合同/`（沁阳平安 5888 万） |
| **3960 纸机**（已暂停） | `02-合同/`（浙江华章 DCS） |
| **电站**（热电站） | `01-进度计划/*电站*`、`02-合同/`、`07-采购跟踪/*电站*` |
| **水处理 / 污水站** | `01-进度计划/*污水*`、`02-合同/`、`07-采购跟踪/*污水*` |
| **制浆车间 / 碎解车间** | `01-进度计划/*制浆*/*碎解*`、`02-合同/`（郑州磊展、英锋） |
| **主厂房 / 干煤棚 / 一号库** | `01-进度计划/月度计划表/2026.08/土建/` |
| **机修车间 / 办公楼 / 宿舍 / 食堂** | 周例会提及 + `02-合同/施工与装修合同/` |

---

## 五、命名规范（强制规则）

### 5.1 文件命名

```text
[日期YYYYMMDD]-[主体]-[事项]-[版本].[扩展名]
```

| 要素 | 规则 | 示例 |
| --- | --- | --- |
| 日期 | 一律 `YYYYMMDD` 前缀，无空格 | `20260815-柯美纸业-电站钢结构补充协议-百世德.pdf` |
| 主体 | 甲方-乙方 或 专业-负责人 | `4600纸机_周汇报_0810.xlsx` |
| 版本 | `v1/v2/v3` 紧贴扩展名前；最新版不写版本号 | `公司信息汇总.xlsx`（当前版） |
| 状态 | 仅限临时区使用 | `【待编辑】【待确认】【待归档】` |

**禁止**：文件名带 `(1)(2)(3)` 下载后缀、`~$`、`.bak`、空格分隔日期。违规由 `check_repo.js` 检出。

### 5.2 目录命名

| 类型 | 格式 | 示例 |
| --- | --- | --- |
| 月度归档 | `YYYY.MM/` | `01-进度计划/月度计划表/2026.08/` |
| 周例会资料（新建） | `YYYYMMDD-YYYYMMDD/` | `各专业汇报内容/20260824-20260830/` |
| 会议日期归档 | `YYYYMMDD/` | `03-例会汇报/…/各专业汇报内容/20260819/` |
| 录音归档 | `YYYY-MM-DD/` | `会议录音/2026-08-12/` |
| 模板/归档区 | `_` 前缀 | `_模板-…`、`_archive/` |

历史周目录（如 `2026824830`、`20260819`）不迁移、不重命名；关联当周资料时沿用既有目录，只有不存在对应历史目录时才新建上述格式。

### 5.3 版本策略

- 同一文档保留 1 个**当前版**（不带版本号）+ 历史版移入 `_archive/`；
- 当前权威版判定：汇总类看"数据截止"字段，制度类看 README/INDEX 标注。

---

## 六、核心工作流

### 6.1 重大节点进度追踪闭环（7 步法）

> 流程文档：[重大节点进度督察工作流程](04-进度督察/通报与处罚存档/重大节点进度督察工作流程.html)

收计划 → 理节点并登记/更新事项（`JK-RW`）→ 日对照 → 双确认 → 草案核验 → 确认签发通报（`JK` 编号）→ 证据归档 → 复查关闭

反馈“已完成”只可将事项更新为“待复查”；未经复查、无证据的事项不得关闭。

### 6.2 三级追责标准

| 级别 | 触发 | 措施 | 签批 |
| --- | --- | --- | --- |
| 🟡 通报 | 当月首次未完成 | 公开通报，限当日内提交整改计划 | 经办人+责任人 |
| 🟠 警告 | 通报后再次未完成 | 计入月度考核，联动处罚存档，抄送行政部+采购部 | +总监确认 |
| 🔴 罚款 | 第三次未完成或重大影响 | 扣减金额/外协按合同罚没，上报公司领导 | +总经理批准 |

### 6.3 周例会汇报链

各专业负责人 → 月度计划表与未关闭事项 → 总经办汇总 → 董事长例会 PPT + 汇报数据 → 经确认的会议纪要 → 工作事项台账更新与下周复盘

---

## 七、编号体系

- **工作事项编号**：`JK-RW-YYYY-NNNN`（RW=任务），每项唯一、不删除不复用；状态和证据以 [工作事项总台账](04-进度督察/工作台账/工作事项总台账.md) 为准。
- **通报编号**：`JK-YYYY-NNNN`（JK=江西柯美，年份+4 位流水，递增不重复），仅在总经办确认正式签发后按 [JK-通报编号流水](04-进度督察/工作台账/JK-通报编号流水.md) 分配；草案不占用编号。
- **会议纪要编号**：`JK-HY-YYYYMMDD`。
- **纸机代号**：`5600`（新建）/ `4600`（搬迁技改）/ `3960`（已暂停）/ `PM1` `PM2`

---

## 八、维护指南（Agent 操作手册）

### 8.1 日常入库（你确认后执行）

1. 从 `00-临时存放/` 取【待归档】文件，按第一节归入对应目录；
2. 按第五节正式命名；手改 Office 版则回写 md 源头；
3. 涉及会议决策、推进反馈、整改回执或通报时，关联或更新工作事项台账，但不得因入库直接关闭事项；
4. 逐个指定文件执行 `git add`，不得使用 `git add -A`；
5. 运行 `node scripts/check_repo.js --staged`。台账有变更时，再运行 `node scripts/check_work_ledger.js`；
6. `node scripts/gen_index.js`，暂存 `INDEX.md`，复核后提交并推送。

### 8.2 卫生检查

```bash
node scripts/check_repo.js --staged  # 提交前：仅检查本次暂存内容
node scripts/check_work_ledger.js    # 工作事项台账有变更时必须通过
node scripts/check_repo.js           # 全库卫生审计：历史告警按第九节 TODO 管理
node scripts/gen_index.js            # 根据已暂存文件重新生成 INDEX.md
```

### 8.3 各专项流程

| 事项 | 操作 |
| --- | --- |
| 新增月度计划 | `01-进度计划/月度计划表/YYYY.MM/`，土建专项放 `土建/` 子目录 |
| 新增周汇报 | `03-例会汇报/董事长例会/各专业汇报内容/YYYYMMDD-YYYYMMDD/`；同周历史目录存在时沿用，不新建重复目录 |
| 会议纪要 | `03-例会汇报/董事长例会/会议纪要/YYYYMMDD-YYYYMMDD/`，命名 `YYYYMMDD-YYYYMMDD-董事长例会纪要-XX版`；编制流程见该目录 README |
| 周例会名单 | `03-例会汇报/董事长例会/周例会名单/`，命名 `周例会名单MMDD.xlsx` |
| 新增合同 | `02-合同/` 对应分箱，命名 `YYYYMMDD-甲方-乙方-事项.pdf` |
| 新增工作事项 | 从已确认的纪要、计划、推进单或督察证据提取，更新 `04-进度督察/工作台账/工作事项总台账.md` |
| 新增通报 | 核验计划、事项台账和证据 → 临时区草案 → 用户确认签发后分配 JK 编号 → `04-进度督察/通报与处罚存档/各专业通报/` |
| 会议录音 | `03-例会汇报/董事长例会/会议录音/YYYY-MM-DD/`，注意体积（见 TODO） |
| 新增参考资料 | 复制入 `参考资料/` 按主题建子目录（静态只读，不参与周例会流程）；删除冗余 zip |

### 8.4 Git 推送与网络代理（网络维护）

**背景**：本机直连 GitHub 的 443 端口不稳定（DNS 会解析到被拦截的节点，连接时通时断），需通过 **v2rayN 代理（SOCKS5，端口 10808）** 推送。

**当前生效配置**：

| 作用域 | 配置项 | 值 | 作用 |
| --- | --- | --- | --- |
| 全局 | `http.proxy` / `https.proxy` | `socks5://127.0.0.1:10808` | 所有仓库推送走代理 |
| 本地（本仓库） | `http.postBuffer` | `524288000` | 500MB 缓冲，防大文件推送断连 |
| 本地（本仓库） | `http.version` | `HTTP/1.1` | 避开 HTTP/2 大流量问题 |

**正常推送（走代理）**：

1. 保持 **v2rayN 开启**（10808 端口监听）；
2. 命令行：`git push origin master`；
3. VS Code：Source Control 面板点「推送」按钮即可 —— VS Code 底层调用系统 `git.exe`，复用上述全局配置，效果与命令行一致。

**回退到非代理（直连）状态**：

```bash
git config --global --unset http.proxy
git config --global --unset https.proxy
```

验证（应无输出）：`git config --global --get http.proxy`

**恢复代理**：

```bash
git config --global http.proxy socks5://127.0.0.1:10808
git config --global https.proxy socks5://127.0.0.1:10808
```

**排查命令**：

```bash
# 测试代理连通性（返回 HTTP:200 即正常）
curl.exe -x socks5://127.0.0.1:10808 -sS -o NUL -w "HTTP:%{http_code}`n" https://github.com
# 测试直连连通性（True=通 / False=被拦截）
Test-NetConnection github.com -Port 443
# 检查代理端口是否监听
Get-NetTCPConnection -State Listen | Where-Object { $_.LocalPort -eq 10808 }
```

> 注意：v2rayN 默认 **10808 = SOCKS5**、**10809 = HTTP**（本机当前仅监听 10808）。若更换端口，同步修改上方两条 `http.proxy` 即可。

### 8.5 Agent 能力体系

| 定制 | 位置 | 用途 |
| --- | --- | --- |
| 常驻规则 | `AGENTS.md`（根目录） | 每次会话自动注入：目录分箱、命名规范、铁律、提交约定 |
| 技能 | `.github/skills/` | `/doc-intake` 文档入库、`/repo-publish` 检查发布、`/weekly-meeting` 周例会归集、`/meeting-minutes` 纪要草案、`/work-tracker` 事项闭环、`/jk-notice` 督察通报 |
| 提示 | `.github/prompts/` | `/report-gap` 本周缺报速查、`/who` 人员岗位速查、`/work-status` 在办事项状态速查 |
| 范围指令 | `.github/instructions/` | 工作事项台账的证据关闭门槛、会议纪要的三版同源与事项移交约束 |
| 会话记录 | `_会话记录/` | Copilot 历史对话与记忆，随仓库迁移（见 8.6） |

聊天输入 `/` 即可调用。当前能力不自动签发通报、不自动处罚，也不依据口头反馈关闭事项；合同台账和自动化强约束另行评估。

### 8.6 会话记录同步（换电脑接续）

VS Code 把聊天记录存在用户目录的 `workspaceStorage/<工作区哈希>/`，不支持改指向项目文件夹，且哈希因机器而异。因此以 `_会话记录/` 作为可移植副本，用脚本双向同步：

```bash
node scripts/sync_chat.js --status     # 查看两侧状态
node scripts/sync_chat.js --export     # 阶段性收尾：VS Code → 仓库
node scripts/sync_chat.js --import     # 换电脑：仓库 → VS Code
```

换电脑流程：`git pull` → 用 VS Code 打开本文件夹一次 → `--import` → 重新加载窗口。详见 [会话记录 README](_会话记录/README.md)。会话内容会随仓库推送，不要在对话中粘贴密码或密钥。

---

## 九、待办事项（TODO）

- [ ] 13 个 `(1)(2)(3)` 重复后缀文件核对去重（`check_repo.js` 清单）
- [ ] 11 个大文件（>10MB：PPT/PDF/录音共约 200MB）评估 Git LFS 或网盘存储
- [ ] 合同台账数据库（从 `02-合同/` 文件名提取金额/日期/双方）
- [ ] 每周例会确认后，持续将新增事项和状态更新回工作事项总台账，并在会前复查未关闭事项
- [ ] 确认机械、生产、财务部周例会汇报负责人（电仪已确认：段伟）
- [ ] 确认陶祥存、邹金艳所属专业
- [ ] `05-政府沟通/` 沟通协调表补充实际数据
- [ ] 安保日报归档机制：`08-安保与应急/保安管理/日报存档/`
- [ ] 3960 纸机暂停的正式决策文件
- [ ] `周例会名单/~$` 锁文件待 Excel 关闭后清理

---

## 十、更新记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-27 | 会话记录随仓库迁移：新增 `_会话记录/` 与 `scripts/sync_chat.js`（导出/导入 Copilot 会话与记忆），根目录白名单与全文索引同步适配（8.6 节） |
| 2026-08-27 | 建立工作事项闭环督办体系：新增工作事项总台账、JK 通报编号流水、台账校验与暂存区卫生检查；新增事项跟踪/会议纪要/督察通报技能及状态速查提示，周例会、入库、发布流程同步接入 |
| 2026-08-27 | Agent 能力体系 Phase 1+2：根部 `AGENTS.md` 常驻规则；`.github/` 技能（doc-intake / repo-publish / weekly-meeting）与提示（report-gap / who）；check_repo 白名单同步（8.5 节） |
| 2026-08-27 | 新增「参考资料/」静态资料区：导入岗位说明书5.10（组织架构图 + 岗位说明书 + Visio/PDF 源文件，238 文件）；发布可检索的 `组织架构总览.html` |
| 2026-08-26 | 会议纪要体系建立：首期纪要（2026824830，总经理版+董事长版+完整版群发）、会议纪要目录 README、周前缀命名规则写入 5.2 与 8.3 |
| 2026-08-25 | 综合治理：9 月月度计划（电站/污水电气）、8.24 周例会汇报（9 份）、周例会名单目录化；复工函（万睿）入库临时区；人员映射补陈立莎/刘萍萍/罗潜/陶祥存/邹金艳；维护指南补周例会名单流程 |
| 2026-08-20 | 新增「8.4 Git 推送与网络代理」维护说明（v2rayN SOCKS5 代理 + 直连回退方案） |
| 2026-08-20 | 目录体系彻底重构（00-09 数字前缀 + _archive + 00-临时存放）；INDEX.md 脚本自动生成；README 瘦身重写；3 个冗余 zip 停止追踪 |
| 2026-08-19 | 应急预案发布（碎解车间事件经验固化） |
| 2026-08-18 | 保安管理文档（规范+日报模板，双人编制） |
| 2026-08-11 | 行政部例会材料移交整合 |

---

*本 README 由总经办维护。归属者：漆江峘（总经理助理）。目录变动时同步更新本文件与 `INDEX.md` 并推送远端。*
